function findProducts(numbers) {
  // body
}

function rangePriceProducts(numbers) {
  // body
}

function sortProducts(numbers, sortingType) {
  // body
}

function paginateProducts(numbers) {
  // body
}
